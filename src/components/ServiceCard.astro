---
const { title, img } = Astro.props;
---
<div class="bg-white rounded-2xl shadow border border-gray-100 flex flex-col items-start px-4 pt-4 pb-0 w-full min-w-52 aspect-[3/4] relative overflow-hidden">
  <h3 class="text-base font-medium mb-2 text-black z-10">{title}</h3>
  <div class="relative w-full">
    <button class="mb-4 px-2 py-1 bg-white text-black rounded-lg border border-gray-200 transition-colors flex items-center gap-2 text-xs font-normal z-10 w-fit cursor-not-allowed opacity-60" disabled>
      Coming soon
    </button>
  </div>
  <div class="absolute left-0 bottom-0 w-full h-2/3 pointer-events-none">
    <div class="absolute left-0 bottom-0 w-full h-full rounded-bl-2xl bg-gradient-to-t from-pink-100 via-white/80 to-transparent" style="filter: blur(4px);"></div>
    <img src={img} alt={title} class="absolute left-0 bottom-0 w-full h-full object-cover z-10" />
  </div>
</div> 