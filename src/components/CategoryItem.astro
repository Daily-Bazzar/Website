---
const { imgSrc, alt, label } = Astro.props;
---
<div class="flex flex-col items-center w-24 md:w-28">
  <img src={imgSrc} alt={alt} class="w-20 h-20 md:w-28 md:h-28 object-contain mb-2 md:mb-1" />
  <span class="mt-2 text-[#222] text-base">{label}</span>
</div> 